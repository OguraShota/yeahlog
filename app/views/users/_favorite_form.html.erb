<div id="favorite-<%= @property.id %>">
  <% if !current_user.nil? && current_user.favorite?(@property) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>